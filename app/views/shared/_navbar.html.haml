%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      %a.navbar-brand{ href: '#' } Lunch Game
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        - if current_user
          %li.nav.navbar-nav
            %span.navbar-text
              Welcome
              = current_user.name
      %ul.nav.navbar-nav.navbar-right
        - if current_user
          %li= link_to 'Log Off', main_app.destroy_user_session_path, method: :delete
